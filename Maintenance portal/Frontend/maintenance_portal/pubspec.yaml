name: maintenance_portal
description: A maintenance portal application for plant maintenance activities.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  assets:
    - Assets/Loading-image.png
    - Assets/logo-image.png
    - Assets/Background-image.png
